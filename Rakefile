#!/usr/bin/env rake
desc "Deletes xcode generated artifacts"
task 'clean:xcode' do
  `find ~/Library/Application\ Support/iPhone\ Simulator -name *ambient* -delete`
  `find ~/Library/Developer/Xcode/DerivedData -name *ambient* -delete`
end

